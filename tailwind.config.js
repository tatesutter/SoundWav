module.exports = {
    darkMode: 'class',
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // <-- This ensures all your components are scanned
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
// This is a basic Tailwind CSS configuration file that sets up the content paths and extends the default theme. You can customize it further based on your project's needs.  